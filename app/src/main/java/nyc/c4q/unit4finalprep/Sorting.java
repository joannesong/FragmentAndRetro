package nyc.c4q.unit4finalprep;

import android.support.annotation.NonNull;

/**
 * Created by joannesong on 1/9/18.
 */

//public class Sorting implements Comparable{
////    int age;
////
////    @Override
////    public int compareTo(@NonNull Object o) {
////        Sorting sorting = (Sorting) other;
////        if(this.age > sorting.age){
////            return 1; // when we are greater than the comparison we return 1
////        }
////        if(this.age == sorting.age){
////            return 0; // when we are equal to the comparison we return 0
////        }
////        if(this.age < sorting.age){
////            return -1; // when we are less than the comparison we return -1
////        }
////    }
//}
